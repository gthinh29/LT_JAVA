// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // Bạn có thể thêm các cấu hình Next.js khác ở đây nếu cần
  // Ví dụ:
  // reactStrictMode: true,
  // swcMinify: true,
};

module.exports = nextConfig;